#in.tile-group.five
  .tile-group-title In-room
  - @ins.each do |m|
    .tile.animated.fadeIn{"data-click" => "transform"}
      .tile-content.image
        %img{:src => "#{m.avatar_image_url}", "id" => "#{m.name}", "alt" => "#{@in_action}"}/
      .brand
        .label.fg-amber #{m.name}
        .badge.available
    / end tile
/ End group

#out.tile-group.three
  .tile-group-title Absent
  - @outs.each do |m|
    %a.tile.half.bg-dark.animated.fadeIn{"data-click" => "transform"}
      .tile-content
        %img{:src => "#{m.avatar_image_url}", "id" => "#{m.name}", "alt" => "#{@out_action}"}/
/ End tile group
